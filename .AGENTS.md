# CadE2E-Harness Agent 指令

修改或调用 Runner 前，端到端或全自动 CAD 测试必须阅读并遵循 [`autocad-e2e`](autocad-e2e/SKILL.md)。

保持 Harness 可从约定 Submodule 路径 `Tests/CadE2E-Harness` 使用。相对 Runner 位置解析消费仓库根目录，项目专用插件和测试程序集保存在消费仓库。

写图测试必须操作图纸副本。每次自动运行都要求语义 PASS Marker 和时间戳 Artifact。
